pipeline {

    agent any

    stages {
		stage('set directory') {
			sh '../Application'
        }

		stage('restore') {
            steps {
				sh 'dotnet restore'
            }
        }

		stage('build'){
			steps {
				sh 'dotnet build'
			}
		}

		stage('test'){
			steps {
				sh 'dotnet test'
			}
		}
    }
}
